# プロジェクト方針

## ユーザーの作業スタイル（本人の明示・恒久ルール）

> **完全自動で、少額でも利益を積み重ねる仕組みを AI で作るのが好き。**

- 技術スタック: **Python / VPS（ConoHa, Ubuntu, systemd）/ Claude Code（headless, cron）**
- 既存の実運用資産: BTC/JPY 自動売買（`/opt/auto-trade`, `trade-btc.service`）
- 好まないもの: 人間が毎日手を動かし続ける必要がある仕組み

## 提案・設計するときの原則

1. **無人運用型を出す。労働置換型を出さない。**
   「人間が毎日1時間やれば儲かります」は提案として却下。運用労力が限りなく 0 に近いものを出す。

2. **必ず律速段階を明示する。**
   自動化はボトルネックを消さず、移動させるだけ。
   ポイ活では「申込ペース制限」、他ビジネスでは多くの場合「集客」が律速になる。
   律速を特定せずに「自動化すれば儲かる」と言わない。

3. **収益見込みは制約を入れた実数で出す。**
   楽観値を出さない。シミュレーションで検証できるものは検証してから数字を出す。
   （例: ポイ活の試算は制約を入れて月¥351,911 → 月¥78,668 に修正された）

4. **初期労力と運用労力を分けて書く。**
   「完全自動」は運用フェーズの話であって、構築工程は自動化できない。ここを混同しない。

5. **AI に最終決定権を与えない。**
   LLM は判断材料を作るだけ。実行判断は Python の決定論ルールが行う。
   金額計算を LLM にさせない。

6. **モデル名・無料枠 RPD をコードに埋め込まない。**
   年単位で変わるため、必ず設定ファイルに外出しする。

7. **安全ゲートをコスト最適化より前に置く。**
   「AI を呼ぶ前に安いフィルタで落とす」は正しいが、安全判定を追い越させてはいけない。

## 提案しない領域

| 対象 | 理由 |
|------|------|
| ポイントサイトのアンケート自動回答・広告自動クリック・アプリ自動起動 | 広告主は「実在の人間が実際に見た・使った・答えた」ことに対価を払っており、自動化はその前提を崩す。規約違反にとどまらない。アンケートは市場調査データも汚染する |
| 投資シグナル配信・売買判断の販売 | 金融商品取引法の投資助言・代理業に該当しうる。無登録営業は違法 |
| AI 生成コンテンツの大量投稿 | 検索エンジンのスパムポリシー違反 |
| スクレイピングデータの無断再配布 | 著作権・利用規約違反 |
| 他人のアカウントを操作する自動化 | ほぼ全サービスの規約違反 |

スクレイピングを行う場合は、robots.txt の尊重・1リクエスト3秒以上の間隔・
User-Agent への連絡先明記・利用規約の事前確認を必須とする。

## モデルの使い分け（BTC プロジェクトの流儀を踏襲）

| タスク | モデル |
|-------|-------|
| ログ確認・設定値変更・単純な修正 | Sonnet |
| 戦略設計・シミュレーション・複雑バグ・月次レビュー | Opus |

---

# このリポジトリの構成

Rails の Pictgram アプリ（`app/`, `config/`, `db/`）と、
ポイ活自動化プロジェクト（`poikatsu/`, `docs/`）が同居している。

## ポイ活自動化プロジェクト

| ドキュメント | 内容 |
|------------|------|
| `docs/poikatsu-automation-design.md` | 全体設計。収益源の分類、DBスキーマ、交換ルート最適化、税務 |
| `docs/poikatsu-multiagent-simulation.md` | マルチエージェント構成の検証結果と、発見した設計バグ |
| `docs/poikatsu-subscription-risk.md` | 継続課金案件の解約忘れリスクの実測と対策 |
| `docs/auto-income-ideas.md` | Python+VPS+Claude Code で完全自動運用できる収益源の比較 |

```bash
python3 -m poikatsu.sim.run all   # 全シミュレーションを実行（依存ゼロ）
```

詳細は `poikatsu/README.md`。
