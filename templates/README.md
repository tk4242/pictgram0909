# templates/

- `template_registry.json` … 支給PPTX（グラフ・図表作成のテンプレート_0602 (1).pptx・133スライド）の
  解析結果。どのスライドがどの図表archetypeかを記録し、生成時に毎回全スライドを読まないためのもの
  （設計書v2.0 §27）。
- **PPTX原本（14.7MB）はリポジトリに含めていない**。サイズと権利（クライアント支給物）のため。
  原本はクライアント支給のファイルを `templates/original_infobank.pptx` として各環境に手動配置する。
  registryのスライド番号は原本のものと一致する。
