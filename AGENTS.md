# AGENTS.md

このリポジトリで作業する AI エージェント共通の指示書です。

**このファイルが唯一の情報源（single source of truth）です。**
Codex は `AGENTS.md` を、Claude Code は `CLAUDE.md` を読みますが、`CLAUDE.md` は
このファイルを参照するだけの薄いファイルです。ルールを変更するときは必ずこのファイルを編集してください。

---

## 0. 作業対象の範囲

両 AI（Codex / Claude Code）が操作してよい対象は次の 2 つです。

| 対象 | 実体 | 詳細 |
| --- | --- | --- |
| GitHub リポジトリ | `tk4242/pictgram0909` | 本ファイルの §1〜§5 |
| VPS（リモートサーバ） | `root@160.251.137.210`（Python 実行環境） | 本ファイルの §6 |
| 作業フォルダ（リポジトリ内） | `ai/shared/` `ai/codex/` `ai/claude/` | 本ファイルの §9 |
| 共有フォルダ（git 管理外） | `~/Desktop/ai-shared/` | 本ファイルの §9 |

どちらも **2 つの AI が同じファイルを触る**前提です。片方が変更した直後にもう片方が
古い状態を前提に動くと壊れるため、**作業開始前に必ず最新状態を取り込む**ルール（§5・§6）を
例外なく守ってください。

そして最も重要な前提: **2 つの AI はお互いのチャットを見ることができません。**
経緯・決定事項・申し送りは `docs/ai-worklog.md`（§8）だけが共有経路です。
**作業前に必ず読み、作業後に必ず追記してください。**

---

## 1. プロジェクト概要

Pictgram — 画像投稿型 SNS（Rails チュートリアル系の学習用アプリ）。

| 項目 | 値 |
| --- | --- |
| フレームワーク | Rails 5.2.1 |
| Ruby | 2.4.1（`.ruby-version` / `Gemfile` の指定） |
| DB | SQLite3 |
| テスト | Minitest（`rails test`）+ Capybara / Selenium（システムテスト） |
| CSS | SCSS + bootstrap-sass |
| JS | CoffeeScript + Turbolinks（Sprockets） |

現状の実装範囲は非常に小さいです。把握しておくべき全体像:

- `app/models/user.rb` — `User`（`name` / `email` のみ、バリデーションなし）
- `app/controllers/users_controller.rb` — `new` / `create`
- `app/controllers/pages_controller.rb` — `index`（root）/ `help`
- `config/routes.rb` — `root 'pages#index'`, `get 'pages/help'`, `resources :users`
- `db/schema.rb` — `users` テーブル（`name`, `email`, timestamps）

---

## 2. 環境セットアップ

```bash
bundle install
bin/rails db:schema:load   # もしくは bin/rails db:migrate
bin/rails server
```

### 重要な前提条件

`Gemfile` は `ruby '2.4.1'` を要求します。**新しい Ruby が入った環境では `bundle install` /
`bundle check` が即座に失敗します。** 実際のエラー:

```
Your Ruby version is 3.3.6, but your Gemfile specified 2.4.1
```

つまり **Ruby 2.4.1 が用意できない環境では、テストを実行した「検証」はできません。**
その場合は次のいずれかを選び、**どちらを選んだかを必ず報告に明記してください。**

- **A. 静的検証のみ**（既定）— コードを読んで指摘する。テスト実行は「未実施」と明記する。
- **B. Ruby を用意する** — rbenv / asdf / Docker で 2.4.1 を入れて実行する。
- **C. Ruby バージョンを上げる** — `Gemfile` と `.ruby-version` の変更は影響が大きいため、
  **勝手に実施せず必ずユーザーに確認すること。**

テストを実行していないのに「動作確認しました」と書いてはいけません。

---

## 3. 検証（verification）コマンド

「終わった」と報告する前に、実行できたものだけを実行し、**実行した / しなかったを区別して報告**します。

```bash
bin/rails test                      # 全テスト（モデル・コントローラ）
bin/rails test test/models          # モデルのみ
bin/rails test test/controllers     # コントローラのみ
bin/rails test:system               # システムテスト（Chrome が必要）
bin/rails routes                    # ルーティング確認
bin/rails runner 'puts User.count'  # DB 疎通確認
```

Rubocop などの Linter はこのリポジトリに導入されていません。導入は勝手に行わないでください。

---

## 4. コーディング規約

既存コードの書き方に合わせることを最優先します。そのうえで:

- インデントは **半角スペース 2 つ**。現在 `users_controller.rb` の `create` は
  インデントが崩れているので、触るときに整えてください。
- `binding.pry` / `debugger` / `puts` によるデバッグ出力を**コミットに残さない**。
- コントローラで Strong Parameters（`params.require(:user).permit(...)`）を使う。
  現状は `params[:user][:name]` を直接読んでいますが、新規コードでは踏襲しないでください。
- ビューは ERB。スタイルは `app/assets/stylesheets/` 配下の該当 `.scss` に書く。
- 日本語コメントは可。ただし既存ファイルのコメント密度に合わせ、自明なコメントは書かない。

---

## 5. Git / GitHub 運用ルール

**両 AI が同じリポジトリの同じファイルを触るため、以下は例外なく守ってください。**

### ブランチ

- `master` に直接コミット・直接 push しない。必ずブランチを切る。
- ブランチ名の先頭で **どの AI が作業したか分かるようにする**:
  - Codex: `codex/<内容>` 例）`codex/user-validation`
  - Claude Code: `claude/<内容>` 例）`claude/user-validation`
- 1 ブランチ = 1 目的。複数の関心事を混ぜない。

### 作業開始前に必ず実行

もう一方の AI が先に push している可能性があります。**作業開始時は必ず最新を取り込む**:

```bash
git fetch origin
git checkout master && git pull origin master
git checkout -b <prefix>/<内容>
```

### push

```bash
git push -u origin <branch-name>
```

ネットワークエラー時のみ、2s → 4s → 8s → 16s のバックオフで最大 4 回リトライします。

### コミットメッセージ

1 行目は変更内容を要約した命令形。末尾に**どの AI が書いたかを示す行を必ず入れます**
（後から相互検証の追跡ができるようにするため）:

```
Add email format validation to User

Co-Authored-By: Codex <codex@openai.com>
```

Claude Code の場合は Claude Code 側の規定の trailer を使ってください。

### コンフリクト

同じファイルを両 AI が編集して衝突した場合、**自分の変更で上書きしない**。
`git fetch origin && git merge origin/master` で取り込み、両方の意図を残す形で解決します。
どちらの実装を採るか判断が割れる場合は、勝手に決めずユーザーに確認してください。

---

## 6. VPS（リモートサーバ）での作業ルール

Python が動作するリモートサーバに対しても、両 AI の操作を許可します。

### 接続情報

```bash
ssh -i ~/.ssh/id_ed25519 root@160.251.137.210
```

- **鍵は各 AI が動いているローカル環境の `~/.ssh/id_ed25519` を使います。**
  秘密鍵の中身をリポジトリ・チャット・コミット・PR に貼り付けることは絶対に禁止です。
- `root` での接続です。**打ったコマンドが即座に本番へ効きます。** 取り消しは効きません。
- クラウド上で動く AI セッション（Claude Code on the web など）には `ssh` クライアントも
  鍵も存在しません。**VPS 作業はローカルで動いている AI が担当してください。**

### 接続直後に必ず行うこと（現状把握）

もう一方の AI が直前に変更している可能性があります。**触る前に読む**:

```bash
# 何が動いているか
systemctl list-units --type=service --state=running

# 対象ファイルの最終更新日時と中身
ls -la <作業ディレクトリ>
stat <対象ファイル>

# 直近のログ（サービス名は環境に合わせる）
journalctl -u <サービス名> -n 100 --no-pager
```

最終更新日時が自分の記憶と食い違う場合、**もう一方の AI が変更しています。**
上書きせず、差分を確認してからユーザーに報告してください。

### ファイルを編集する前に必ずバックアップ

```bash
cp <対象ファイル> <対象ファイル>.bak.$(date +%Y%m%d_%H%M%S)
```

バックアップを取らずに本番ファイルを編集してはいけません。
編集後は必ず構文チェックを通します:

```bash
python3 -m py_compile <対象ファイル>
```

### 確認なしにやってはいけないこと

以下は**ユーザーの明示的な指示があるときだけ**実行します。勝手に判断しないこと。

- サービスの再起動・停止（`systemctl restart` / `stop`）
- `pip install` / `apt install` によるパッケージの追加・更新
- ファイル・ディレクトリの削除（`rm`）
- 常駐プロセスの `kill`
- cron / systemd timer の追加・変更
- ファイアウォール・SSH 設定の変更
- 実運用の設定値（金額・閾値・API キーなど）の変更

読み取り専用のコマンド（`cat`, `ls`, `tail`, `journalctl`, `grep`, `ps`, `df`）は
確認なしで実行して構いません。**迷ったらまず読む。**

### 作業後に必ず報告すること

1. 実行したコマンドを**そのまま**（要約せず、実際に打った文字列で）
2. 変更したファイルとバックアップ先のパス
3. 構文チェック・動作確認の結果
4. サービスを再起動したかどうか（していなければ「未反映」と明記）

### VPS とリポジトリの関係

VPS 上のファイルは、原則としてこの GitHub リポジトリの管理対象ではありません。
**VPS で直接編集した内容は、そのままではリポジトリに残りません。**
恒久的に残すべき変更は、ユーザーに確認のうえリポジトリ側にも反映してください。

---

## 7. AI 間の相互検証プロトコル

Codex と Claude Code が**同じファイルを見て、お互いの作業を検証する**ための手順です。

### 基本の流れ

```
実装する AI                        検証する AI
─────────────                      ─────────────
ブランチを切る
実装する
検証コマンドを実行
push する            ──────────▶   git fetch && そのブランチを checkout
                                   差分を読む（git diff master...<branch>）
                                   検証コマンドを実行
                                   指摘を返す
指摘に対応            ◀──────────
```

### 実装した側が、報告に必ず含めること

1. **何を変更したか** — ファイル名と変更の意図。
2. **実行した検証** — 実際に走らせたコマンドとその結果（成功 / 失敗）。
3. **実行できなかった検証** — 理由つきで明記（例: Ruby 2.4.1 が無く `bundle install` 不可）。
4. **判断に迷った点** — もう一方の AI に見てほしい箇所。

### コマンドで検証させる（推奨。プロンプトを貼る必要がない）

Claude Code と Codex は MCP で接続できます。セットアップはローカルで 1 回:

```bash
bash ai/bootstrap.sh
```

（共有フォルダ作成と Codex 連携をまとめて実行します。個別に実行する場合は
`ai/setup-desktop.sh` と `ai/setup-codex-integration.sh`）

接続後は、ターミナルから 1 コマンドで相互検証が回ります。

```bash
bash ai/codex-review.sh master        # master との差分を Codex がレビュー
bash ai/codex-review.sh --uncommitted # コミット前の変更をレビュー
```

下のチェック項目を自動で Codex に渡し、結果を `ai/codex/reviews/` に保存します。
**保存されたレビュー結果は、`docs/ai-worklog.md` に要約を追記して初めて共有されます。**

#### 検証済みの事実（誤情報に注意）

ネット上の解説には次の誤りが見られます。実際に確認した結果:

| よくある記述 | 実際 |
| --- | --- |
| `npm i -g @openai/codex-cli` | **存在しない**（npm 404）。正しくは `@openai/codex` |
| Codex は Claude Code のプラグイン | **違う。** 独立した CLI。`claude plugin install` はマーケットプレイスからの取得で npm パッケージは扱えない |
| `claude plugin reload` | **存在しないコマンド** |
| API 従量課金が必須 | **必須ではない。** `codex login` は ChatGPT アカウント（Plus / Pro）でのログインに対応。API キーを使う場合のみ `codex login --with-api-key` |

正しい連携経路は `codex mcp-server`（stdio）を `claude mcp add` で登録することです。

### 検証する側が、必ず確認すること

```bash
git fetch origin
git diff master...origin/<相手のブランチ>   # 差分を全部読む
```

チェック項目:

- [ ] `binding.pry` / `debugger` / デバッグ用 `puts` が残っていないか
- [ ] テストが変更に追随しているか（新しい分岐にテストがあるか）
- [ ] ルーティングヘルパー名が `config/routes.rb` の定義と一致しているか
- [ ] Strong Parameters を通しているか（`params[:user][:x]` 直読みでないか）
- [ ] `db/schema.rb` とマイグレーションの整合が取れているか
- [ ] 報告された「検証済み」の主張が、実際に実行可能なものだったか

**相手の報告を鵜呑みにしないこと。** 「テストが通った」と書かれていても、
この環境でテストが実行できるかを自分で確かめてから受け入れてください。

### 検証結果の残し方

上記チェック項目を **確認できたもの / 問題が見つかったもの / 確認できなかったもの** に
分けて書きます。「LGTM」だけの応答は禁止です。何を見て問題なしと判断したかを書いてください。

出力先は 3 つ。**ユーザーへの応答**、PR があれば **PR コメント**、
そして **`docs/ai-worklog.md`（§8）に必ず追記** します。
ワークログに残さない指摘は、もう一方の AI に届きません。

---

### VPS 上の変更を相互検証する場合

GitHub と違って差分が自動で残らないため、**検証する側は自分の目で現物を確認します。**

```bash
ssh -i ~/.ssh/id_ed25519 root@160.251.137.210
diff <対象ファイル>.bak.<タイムスタンプ> <対象ファイル>   # バックアップとの差分
python3 -m py_compile <対象ファイル>                      # 構文
journalctl -u <サービス名> -n 100 --no-pager              # 変更後の挙動
```

バックアップが存在しない変更は、**検証不能として差し戻してください。**
「バックアップを取らずに本番を編集した」こと自体が指摘対象です。

---

## 8. 作業内容・会話の共有（`docs/ai-worklog.md`）

**Codex と Claude Code は、お互いのチャット画面を見ることができません。**
片方のセッションでユーザーと交わした会話・決めた方針・見つけた問題は、
そのままではもう片方に一切伝わりません。

そこで **`docs/ai-worklog.md` を「2 つの AI 共通の記憶」として使います。**
リポジトリにコミットされるため、両 AI が同じ内容を読めます。

### 読むタイミング（必須）

作業を始める前に、以下を必ず実行します。

```bash
git fetch origin
git log --oneline -10                 # もう一方の AI の直近コミット
cat docs/ai-worklog.md                # これまでの経緯と申し送り
```

`docs/ai-worklog.md` の**冒頭「現在の状況」と、末尾の直近 3 エントリは必ず読んでください。**
ここを読まずに作業を始めると、もう一方の AI が既に解決した問題を蒸し返したり、
決定済みの方針に反する実装をすることになります。

### 書くタイミング（必須）

以下のいずれかを行ったら、**同じコミットの中で** `docs/ai-worklog.md` に追記します。

- 実装・修正した
- 相手の作業を検証して指摘した
- VPS 上のファイルを変更した
- ユーザーと方針を決めた（「この設計で行く」「これはやらない」など）
- 調べた結果、重要なことが分かった（環境の制約、再現しないバグ、など）

### エントリの書式

ファイル末尾に追記します。**過去のエントリは書き換えないでください。**

```markdown
### YYYY-MM-DD / <Codex|Claude Code> / <ブランチ名>

**やったこと**
- <変更したファイルと意図>

**検証**
- 実行した: `<コマンド>` → <結果>
- 実行できなかった: `<コマンド>` → <理由>

**決めたこと / 申し送り**
- <もう一方の AI に伝えるべき方針・判断・注意点>

**次にやってほしいこと**
- <もう一方の AI への依頼。無ければ「なし」>
```

「やったこと」だけ書いて「検証」を空にしてはいけません。
検証していないなら「未実施」と理由を書きます（§3・§7 と同じ原則です）。

### 冒頭「現在の状況」の更新

ファイル冒頭の「現在の状況」セクションは**スナップショット**です。
状況が変わったら**上書きして最新に保ってください**（ここだけは書き換え可）。
時系列の記録は下のエントリ側に残ります。

---

## 9. 作業フォルダの構成と所有権

成果物・調査メモ・下書きの置き場です。アプリケーションコードとは分離しています。

### リポジトリ内: `ai/`

| ディレクトリ | 所有者 | 他方の権限 |
| --- | --- | --- |
| `ai/shared/` | 両方 | **読み書き可** |
| `ai/codex/` | Codex | **読み取りのみ** |
| `ai/claude/` | Claude Code | **読み取りのみ** |

- **相手のフォルダのファイルを書き換え・削除してはいけません。**
  問題を見つけたら `docs/ai-worklog.md` に指摘として書きます。
- `ai/shared/` に置いたファイルは、本文冒頭に**どちらが書いたか**を明記します。
- ここにファイルを追加・更新したら、`docs/ai-worklog.md` にも 1 行残します。
  **記録しないと、もう一方の AI はファイルの存在に気づけません。**

詳細は [`ai/README.md`](./ai/README.md)。

### リポジトリ外: `~/Desktop/ai-shared/`

**リポジトリに入れてはいけないもの**の置き場です。git 管理外。

| ディレクトリ | 用途 |
| --- | --- |
| `inbox/` | ユーザーが両 AI に渡すファイル |
| `vps-logs/` | VPS から取得したログ・設定の控え |
| `codex/` `claude/` | 各 AI の出力置き場（相手のものは読むだけ） |

作成は `bash ai/setup-desktop.sh` をローカルで 1 回実行します。

### リポジトリに置いてはいけないもの

- 秘密鍵・API キー・パスワード（`~/.ssh/id_ed25519` の中身を含む）
- 個人情報を含むログ
- 巨大なバイナリ・データダンプ

これらは `~/Desktop/ai-shared/` に置き、**パスだけを共有**します。

---

## 10. 既知の問題（着手前に把握しておくこと）

このリポジトリには現時点で以下の問題があります。**関係ないタスク中に勝手に直さないでください。**
直す場合は独立したブランチで行います。

1. **`app/controllers/users_controller.rb:7` に `binding.pry` が残っている**
   ユーザー登録リクエストが pry のプロンプトで停止します。
   `UsersControllerTest` を含むテスト実行も止まります。最優先の修正候補。

2. **`test/controllers/users_controller_test.rb` のルーティングヘルパーが存在しない**
   テストは `users_new_url` を使っていますが、`config/routes.rb` は `resources :users` のみ。
   生成されるヘルパーは `new_user_url` です。このテストは `NameError` で失敗します。

3. **`User` モデルにバリデーションが無い**
   `name` / `email` が空でも `save` が成功し、`UsersController#create` の
   `if @user.save` の else 分岐に到達しません。

4. **リポジトリに不要ファイルがコミットされている**
   `.DS_Store`, `app/.DS_Store`, `db/development.sqlite3`, 空ファイル `pictgram`, `pivtgram`。
   `.gitignore` にもこれらの指定がありません。

5. **Ruby 2.4.1 / Rails 5.2 は共にサポート終了**
   セキュリティ更新がありません。バージョン更新は影響範囲が大きいため、
   ユーザーの明示的な指示なしに実施しないでください。

---

## 11. やってはいけないこと

**リポジトリ**

- `master` への直接 push
- ユーザーの指示なしでの Ruby / Rails バージョン変更
- ユーザーの指示なしでの PR 作成
- テストを実行していないのに「動作確認済み」と報告すること
- もう一方の AI の変更を、確認せずに上書き・revert すること
- `config/master.key` や認証情報をコミットに含めること
- `docs/ai-worklog.md` を読まずに作業を始めること
- `docs/ai-worklog.md` の過去エントリを書き換え・削除すること
- もう一方の AI のフォルダ（`ai/codex/` `ai/claude/`）のファイルを書き換え・削除すること
- 秘密鍵・API キー・個人情報をリポジトリに置くこと

**VPS**

- バックアップを取らずに本番ファイルを編集すること
- ユーザーの指示なしでのサービス再起動・パッケージ導入・ファイル削除
- 秘密鍵・API キー・認証情報をリポジトリやチャットに出力すること
- 実行したコマンドを省略して報告すること
